如何手写.qrc： 
首先建立资源文件前缀，对应代码中虚拟路径的前缀。此处默认使用 "/" 前缀，请在 qresource 标签内添加资源文件。 
资源文件写法示例："path/to/resource"，其中 path/to/resource 是相对于.qrc文件的路径。 
如果不使用别名，代码中访问路径为前缀加相对路径，例如 ":/path/to/resource"。 如果使用别名，例如 "path/to/resource"，代码中访问路径为 ":/alias-name"。
.qrc将自动包含该文档, 请问将该文本以注释形式写入.qrc中, 这可能会导致其中的实例写法被错误识别为真实内容